require("toggleterm").setup {
    size = 20,
    open_mapping = [[<c-\>]],

    start_in_insert = true,
    persist_mode = true,
    persist_size = true,

    close_on_exit = true,
    direction = 'float',
}
